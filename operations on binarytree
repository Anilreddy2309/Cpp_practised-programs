#include<iostream>
#include<stdlib.h>
using namespcae std;
 
 
void insert(int,int);
int tree[40],t=1,s,x,i;
main()
{
	int ch,y;
	for(i=1;i<40;i++)
	tree[i]=-1;
	while(1)
    {
    cout<<"insertion operation\n"
    cin>>ch;
    switch(ch)
    {
		case 1: 
		   cout<<Enter the element to insert";
	       cin>>ch;
	       insert(1,ch)
	       break;
    }
